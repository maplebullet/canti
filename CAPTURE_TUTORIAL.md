# 抓取校园网认证请求的保姆级教程

本教程手把手教您如何获取学校认证系统的完整请求信息，以便将Canti迁移到您的学校。

## 准备工作

### 需要的工具
- ✅ 任意现代浏览器（Chrome、Edge、Firefox等）
- ✅ 能够正常登录的校园网账号
- ✅ 连接到校园网

### 推荐浏览器
**Chrome或Edge浏览器**（最推荐，本教程基于此）

## 第一步：打开浏览器开发者工具

### 方法1：使用快捷键（最快）
按键盘上的 **F12** 键

### 方法2：使用菜单
1. 点击浏览器右上角的三个点 `⋮`
2. 选择"更多工具" → "开发者工具"

### 方法3：右键菜单
1. 在网页任意位置右键
2. 选择"检查" 或 "Inspect"

**成功后会看到：**
- 浏览器底部或右侧出现开发者工具面板
- 有"元素"、"控制台"、"网络"等标签

## 第二步：切换到网络标签

1. 在开发者工具中，找到 **"网络"** 或 **"Network"** 标签
2. 点击切换到该标签
3. 确保左上角的 🔴 **录制按钮是红色的**（表示正在录制）
   - 如果是灰色的，点击一下让它变红

**提示：** 如果看到很多请求，可以点击 🚫 清除按钮清空列表

## 第三步：打开校园网登录页面

1. 在地址栏输入您学校的校园网登录地址
   - 例如：`http://10.145.255.21`
2. 按回车访问

**重要：** 保持开发者工具打开状态！

## 第四步：进行登录操作

### 4.1 清空网络记录（重要！）

在登录前，点击开发者工具"网络"标签中的 🚫 **清除按钮**，清空所有记录。

### 4.2 输入用户名和密码

1. 在登录页面输入您的学号
2. 输入密码
3. **先不要点登录！**

### 4.3 开始录制并登录

1. 确认"网络"标签是打开的
2. 确认录制按钮 🔴 是红色的
3. 现在点击 **"登录"** 按钮

### 4.4 观察网络请求

登录后，"网络"标签中会出现很多请求，这就是我们要抓取的！

## 第五步：找到关键请求

### 5.1 识别get_challenge请求

在网络请求列表中，找到包含以下关键词的请求：
- 🔍 `get_challenge`
- 🔍 `challenge`
- 🔍 或其他与认证相关的名称

**如何找：**
1. 看"名称"（Name）列
2. 或者使用搜索框，输入"challenge"

**找到后，点击该请求**

### 5.2 查看请求详情

点击后，右侧会显示详细信息：

#### 标签页说明：
- **标头**（Headers）：请求的URL、方法等
- **预览**（Preview）：响应内容的预览
- **响应**（Response）：原始响应内容

### 5.3 记录get_challenge信息

**切换到"标头"（Headers）标签，记录：**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 第一步：get_challenge请求
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

请求URL: 
  [复制"请求URL"右边的完整地址]
  例如：http://10.145.255.21/cgi-bin/get_challenge?callback=jQuery...

请求方法: 
  GET / POST
  [看"请求方法"一栏]

查询字符串参数（Query String Parameters）:
  [展开"查询字符串参数"部分，每一项都记录]
  
  callback: 
  username: 
  ip: 
  _: 
  [其他所有参数...]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 5.4 记录challenge响应

**切换到"响应"（Response）标签：**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 第一步响应：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[完整复制响应内容]
例如：
jQuery11240652393442636386_1765621983080({
    "challenge": "b33554ce31389584648b62320ec487fb4c09df2a59e27b47bc60df1341c0f9c7",
    "client_ip": "10.242.182.12",
    "ecode": 0,
    ...
})

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 第六步：找到登录请求

### 6.1 识别登录请求

继续在网络列表中查找：
- 🔍 `srun_portal`
- 🔍 `login`
- 🔍 `portal`
- 🔍 或包含"action=login"的请求

**这是最关键的请求！**

### 6.2 记录登录请求信息

点击该请求，记录以下信息：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 第二步：登录请求（最重要！）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

请求URL: 
  [完整URL]
  例如：http://10.145.255.21/cgi-bin/srun_portal?action=login&...

请求方法: 
  GET / POST

查询字符串参数（或表单数据）:
  [记录每一个参数！]
  
  action: 
  username: 
  password: 
  ac_id: 
  ip: 
  chksum: 
  info: 
  n: 
  type: 
  [所有其他参数...]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**特别注意password参数：**
```
原始密码: [您输入的密码]
请求中的password: [请求中实际发送的值]

这两个不一样！说明密码被加密了。
```

### 6.3 记录登录响应

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 第二步响应：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[完整响应内容]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 第七步：查找密码加密方式

密码加密逻辑通常在网页的JavaScript代码中。

### 7.1 查看页面源代码

1. 回到登录页面
2. 右键点击页面空白处
3. 选择"查看网页源代码"
4. 或按 **Ctrl + U**

### 7.2 搜索加密相关代码

在源代码中按 **Ctrl + F** 搜索：
- `password`
- `encrypt`
- `md5`
- `sha1`
- `base64`

### 7.3 查找JS文件

源代码中通常会引用JS文件：
```html
<script src="login.js"></script>
<script src="encrypt.js"></script>
```

点击这些链接，查看JS文件内容，搜索加密函数。

### 7.4 记录加密方法

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 密码加密方式：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

加密算法: MD5 / SHA1 / Base64 / 其他

加密方式（复制相关代码）:
  例如：
  password = md5(challenge + password)
  或
  password = base64(hmac_md5(password, challenge))

相关JS代码片段:
  [复制找到的加密函数代码]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 第八步：查找其他参数的计算方式

某些系统需要计算`chksum`、`info`等参数。

### 8.1 在控制台查看

1. 开发者工具切换到"控制台"（Console）标签
2. 登录前，输入以下代码并回车：
```javascript
// 监听所有网络请求
window.XMLHttpRequest.prototype.open = (function(open) {
    return function(method, url) {
        console.log('请求:', method, url);
        return open.apply(this, arguments);
    };
})(window.XMLHttpRequest.prototype.open);
```

### 8.2 在JS代码中搜索

搜索关键词：
- `chksum`
- `info`
- `checksum`
- `calc` 或 `calculate`

### 8.3 记录计算方法

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 特殊参数计算方式：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

chksum计算方式:
  [代码或公式]

info计算方式:
  [代码或公式]

其他参数:
  [说明]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 第九步：查看状态查询和登出

### 9.1 查看在线状态

登录成功后：
1. 保持开发者工具打开
2. 清空网络记录 🚫
3. 刷新页面或查看"在线用户"信息

记录状态查询请求：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 第三步：状态查询
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

请求URL: 

请求方法: 

参数:

响应:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 9.2 点击登出

1. 清空网络记录
2. 点击"登出"或"下线"按钮
3. 记录登出请求

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 第四步：登出请求
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

请求URL: 

请求方法: 

参数:

响应:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 第十步：整理并提交信息

### 10.1 创建完整记录文档

将所有信息整理成文档，格式如下：

```markdown
# 我的学校认证系统信息

## 基本信息
- 学校：XXX大学
- 认证系统类型：SRun / Dr.COM / 其他
- 服务器地址：http://10.145.255.21

## 认证流程

### 步骤1：获取Challenge
- URL: 
- 方法: 
- 参数: 
- 响应: 

### 步骤2：登录
- URL: 
- 方法: 
- 参数: 
- 密码加密方式: 
- 响应: 

### 步骤3：状态查询
- URL: 
- 方法: 
- 参数: 
- 响应: 

### 步骤4：登出
- URL: 
- 方法: 
- 参数: 
- 响应: 

## 加密算法详情
[粘贴相关JS代码]

## 特殊参数计算
[说明chksum、info等参数的计算方式]
```

### 10.2 提交信息的方式

**方式1：创建Issue**
1. 访问 https://github.com/lensferno/canti/issues
2. 点击"New Issue"
3. 标题：`[迁移请求] XXX学校SRun系统适配`
4. 粘贴整理好的信息
5. 提交

**方式2：在当前PR评论**
直接回复，粘贴整理好的信息

**方式3：保存为文件**
保存为 `my_school_auth.md`，发送给开发者

## 常见问题

### Q1: 网络标签中请求太多，怎么找？

A: 
1. 在登录前点击清除按钮 🚫
2. 使用过滤器，只显示"XHR"或"Fetch"类型
3. 使用搜索框搜索关键词

### Q2: 看不到请求内容？

A: 
- 确保在点登录之前开发者工具就已经打开
- 确保录制按钮 🔴 是红色的
- 尝试刷新页面重新登录

### Q3: 响应是乱码？

A: 
- 这可能是压缩或加密的响应
- 查看"响应头"中的`Content-Encoding`
- 浏览器通常会自动解压，查看"预览"标签

### Q4: 密码字段是****，看不到？

A: 
- 在请求中发送的是加密后的密码，不是原始密码
- 需要从JS代码中找到加密函数

### Q5: JS代码看不懂？

A: 
- 复制整个加密相关的函数
- 提供函数名称和调用位置
- 开发者会帮您分析

## 快速检查清单

在提交信息前，检查是否包含：

- [ ] Challenge获取请求的完整URL和参数
- [ ] Challenge响应的完整内容
- [ ] 登录请求的完整URL和所有参数
- [ ] 登录响应的内容
- [ ] 密码加密方式（至少是猜测）
- [ ] 特殊参数（chksum、info等）的说明
- [ ] 状态查询请求（如果有）
- [ ] 登出请求（如果有）
- [ ] 相关的JS代码片段

## 示例：完整记录

```
学校：某某大学
系统：SRun V1.18

━━━ Challenge请求 ━━━
URL: http://10.145.255.21/cgi-bin/get_challenge
参数:
  callback=jQuery123456789_1234567890
  username=2025020632
  ip=10.242.182.12
  _=1234567890

响应:
jQuery123456789_1234567890({
  "challenge": "abc123...",
  "ecode": 0
})

━━━ 登录请求 ━━━
URL: http://10.145.255.21/cgi-bin/srun_portal
参数:
  action=login
  username=2025020632
  password={MD5}a1b2c3d4...（加密后）
  ac_id=1
  ip=10.242.182.12
  chksum=xyz789...
  info={Base64}eyJ1c2...
  n=200
  type=1

加密方式：
password = "{MD5}" + md5(challenge + password)
chksum = sha1(challenge + username + ...)
info = base64(json_encode({...}))

[附加JS代码...]
```

## 完成！

收集完这些信息后，开发者就能够：
1. 理解您学校的认证流程
2. 实现对应的认证模块
3. 让Canti支持您的学校

如有疑问，随时在PR中提问！
